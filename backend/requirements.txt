# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Config / validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- OCR ---
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.4.0
# paddleocr==2.8.1        # optional, uncomment if OCR_ENGINE=paddleocr
# paddlepaddle==2.6.2     # required alongside paddleocr

# --- LLM providers (interchangeable) ---
anthropic==0.34.2
openai==1.51.0

# --- RAG (Slice 1: TF-IDF; Slice 2 adds pgvector) ---
scikit-learn==1.5.2

# --- DB (wired for Slice 2 persistence) ---
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
alembic==1.13.3

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
